1. Java
    ~~~java
    import java.util.*;
    import java.util.stream.*;
    public class Main{
        public static void main(String[] args){
            Scanner sc=new Scanner(System.in);
            final int N=sc.nextInt();
            final int K=sc.nextInt();
            final int P=sc.nextInt();sc.nextLine();
            String str=sc.nextLine();
            List<String> sList=Arrays.stream(str.split(" "))
                .sorted()
                .collect(Collectors.toList());
            List<String> list=new ArrayList<>();
            String temp="";
            for(int i=0;i<sList.size();i++){
                temp+=sList.get(i)+" ";
                if((i+1)%K==0||i+1==sList.size()){
                    list.add(temp);
                    temp="";
                }
            }
            temp=list.get(P-1);
            String[] tArr=temp.split(" ");
            for(String s:tArr){
                System.out.println(s);
            }
            sc.close();
        }
    }
    ~~~
    ---
1. Python
    ~~~python
    list=[len(input()) for i in range(3)]
    print(max(list))
    ~~~
    ---
1. C#
    ~~~c#
    using System;
    using System.Collections.Generic;
    using System.Linq;
    namespace Main{
        class Program{
            static void Main(string[] args){
                var list = new List<int>();
                for(int i = 0; i < 3; i++)
                {
                    list.Add(System.Console.ReadLine().Length);
                }
                Console.WriteLine(list.Max());
            }
        }
    }
    ~~~
    ---
1. Java
    ~~~java
    import java.util.*;
    public class Main{
        public static void main(String[] args){
            Scanner sc=new Scanner(System.in);
            int p1=sc.nextInt();
            int p2=sc.nextInt();
            int p3=sc.nextInt();
            int p4=sc.nextInt();
            int[] eArr=new int[4];
            int[] fArr=new int[2];
            for(int i=0;i<4;i++){
                eArr[i]=sc.nextInt();
            }
            for(int i=0;i<2;i++){
                fArr[i]=sc.nextInt();
            }
            if(eArr[p1-1]<eArr[p2-1]&&eArr[p3-1]<eArr[p4-1]){
                sub(fArr,p1,p3);
            }else if(eArr[p1-1]<eArr[p2-1]&&eArr[p3-1]>eArr[p4-1]){
                sub(fArr,p1,p4);
            }else if(eArr[p1-1]>eArr[p2-1]&&eArr[p3-1]<eArr[p4-1]){
                sub(fArr,p2,p3);
            }else if(eArr[p1-1]>eArr[p2-1]&&eArr[p3-1]>eArr[p4-1]){
                sub(fArr,p2,p4);
            }
        sc.close();
        }
        static void sub(int[] fArr,int a,int b){
            if(a>b){
                int temp=a;
                a=b;
                b=temp;
            }
            if(fArr[0]<fArr[1]){
                System.out.printf("%d%n%d",a,b);
            }else{
                System.out.printf("%d%n%d",b,a);
            }
        }
    }
    ~~~
    ---
1. Python
    ~~~python
    val=int(input())
    if 40<=val<=60:
        print("OK")
    else:
        print("NG")
    ~~~
    ---
1. C#
    ~~~c#
    using System;
    namespace Main{
        class Program{
            static void Main(string[] args)
            {
                int N = int.Parse(Console.ReadLine());
                if (40 <= N && N <= 60)
                {
                    Console.WriteLine("OK");
                }
                else
                {
                    Console.WriteLine("NG");
                }
            }
        }
    }
    ~~~
    ---
1. Java
    ~~~java
    import java.util.*;
    public class Main{
        public static void main(String[] args){
            Scanner sc=new Scanner(System.in);
            final int N=sc.nextInt();
            String[][] sArr=new String[N][N];
            for(int i=0;i<sArr.length;i++){
                for(int j=0;j<sArr[0].length;j++){
                    if(i==j){
                        sArr[i][j]="-";
                    }
                }
            }
            int round=((N*N)-N)/2;
            for(int i=0;i<round;i++){
                int f1=sc.nextInt();
                int f2=sc.nextInt();
                sArr[f1-1][f2-1]="W";
                sArr[f2-1][f1-1]="L";
            }
            for(int i=0;i<sArr.length;i++){
                for(int j=0;j<sArr[0].length;j++){
                    System.out.print(sArr[i][j]);
                    if(!(j+1==sArr[0].length)){
                        System.out.print(" ");
                    }
                }
                System.out.println();
            }
            sc.close();
        }
    }
    ~~~
    ---
1. total code
    - Java x3
    - Python x2
    - C# x2